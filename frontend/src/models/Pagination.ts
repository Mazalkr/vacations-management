class Pagination {  
    public page?: number; // offset - from which index to start? depends on page number
    public limit?: number; // how many cards per page (the demand in the project: 10)
}

export default Pagination;